pip install numpy==1.19.2
pip install pandas==0.25.3
pip install matplotlib==3.1.1
pip install nltk==3.4.5
pip install scikit-learn==0.22.2.post1
pip install seaborn==0.10.1
pip install xgboost==1.0.2
pip install pyspark==2.4.5

install all the packages and then double click on 'download_nltk.bat'
then it will open window and in window click on dowwnload button and 
wait for sometime to allow it to download all packages and once all
downloaded then window wwill to green then u can close window and run project

 